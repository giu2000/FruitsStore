import React from 'react';

const CounterProductsCart = props => {
    return(
        <div>
            <p>Counter Cart {props.counter}</p>
        </div>
    )
}

export default CounterProductsCart;